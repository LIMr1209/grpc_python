syntax = "proto3";

package server_stream;

service server_stream {
    rpc request(Request) returns(stream Response) {}  // 客户端接收流
}


message Request {
    string data = 1;
}

message Response {
    string result = 1;
}